import React, { Component } from 'react';
import { Wrapper } from './styled';
// import messages from './messages';

class NotFoundPage extends Component {
    render() {
        return (
            <Wrapper>NotFoundPage</Wrapper>
        );
    }
}

export default NotFoundPage;